- name: Demo on Loops
  hosts: localhost
  connection: Local
  tasks:
    - name: Print Fruit Name
      ansible.builtin.debug:
        msg: Fruit Name - {{ item }}
      loop:
        - apple
        - orange
        - grape
        - banana




## The variable is given by ansible